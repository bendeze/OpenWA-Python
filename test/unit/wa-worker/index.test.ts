/// <reference types="jest" />

// A basic configuration test for wa-worker
describe('WA Worker Jest Configuration', () => {
  it('should run tests successfully', () => {
    expect(true).toBe(true);
  });
});
